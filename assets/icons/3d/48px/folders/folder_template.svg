<!-- 3D Material Design Folder Icon Template - 48px -->
<!-- Usage: Replace STOP0, STOP1, STOP2, STOP3 with your category colors -->
<!-- Add variant-specific floating indicator in the top layer -->

<svg width="48" height="48" viewBox="0 0 48 48" xmlns="http://www.w3.org/2000/svg">
  <defs>
    <!-- 4-stop gradient for 3D depth -->
    <linearGradient id="folderGradient" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" style="stop-color:#STOP0;stop-opacity:1" />
      <stop offset="35%" style="stop-color:#STOP1;stop-opacity:1" />
      <stop offset="65%" style="stop-color:#STOP2;stop-opacity:1" />
      <stop offset="100%" style="stop-color:#STOP3;stop-opacity:1" />
    </linearGradient>
    
    <!-- Tab gradient (slightly lighter) -->
    <linearGradient id="tabGradient" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" style="stop-color:#STOP0;stop-opacity:0.95" />
      <stop offset="100%" style="stop-color:#STOP1;stop-opacity:0.95" />
    </linearGradient>
    
    <!-- Shadow filters -->
    <filter id="bodyShadow" x="-50%" y="-50%" width="200%" height="200%">
      <feDropShadow dx="0" dy="4" stdDeviation="8" flood-color="#000000" flood-opacity="0.25"/>
    </filter>
    <filter id="tabShadow" x="-50%" y="-50%" width="200%" height="200%">
      <feDropShadow dx="0" dy="2" stdDeviation="4" flood-color="#000000" flood-opacity="0.20"/>
    </filter>
    <filter id="indicatorShadow" x="-50%" y="-50%" width="200%" height="200%">
      <feDropShadow dx="0" dy="1" stdDeviation="2" flood-color="#000000" flood-opacity="0.15"/>
    </filter>
  </defs>
  
  <!-- Tab Layer (elevation 2px) -->
  <g filter="url(#tabShadow)">
    <path d="M4 12 L16 12 L20 8 L44 8 L44 16 L4 16 Z" fill="url(#tabGradient)" />
    <!-- Tab highlight -->
    <path d="M4 12 L16 12 L20 8 L44 8" stroke="#FFFFFF" stroke-width="1" opacity="0.3" fill="none" />
  </g>
  
  <!-- Body Layer (elevation 4px) -->
  <g filter="url(#bodyShadow)">
    <path d="M4 16 L44 16 L44 40 L4 40 Z" fill="url(#folderGradient)" />
    <!-- Body highlight -->
    <path d="M4 16 L44 16" stroke="#FFFFFF" stroke-width="1" opacity="0.2" fill="none" />
  </g>
  
  <!-- Variant Indicator (elevation 1px) -->
  <g filter="url(#indicatorShadow)">
    <!-- Add variant-specific floating element here -->
    <!-- Regular: No indicator -->
    <!-- Encrypted: Lock icon -->
    <!-- Shared: Share icon -->
    <!-- Root: Home icon -->
  </g>
</svg>